
import { allowContentFor } from 'dummy/helpers/allow-content-for';
import { module, test } from 'qunit';

module('Unit | Helper | allow content for');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = allowContentFor([42]);
  assert.ok(result);
});

